# 青藤社区读书会网站 · 项目规矩

- 纯静态站：只有 HTML、CSS 和 data/ 里的示例数据，不用框架、不引外部脚本、不接后台。
- 页面文字全部用简体中文；每页头部都要有 `<meta charset="utf-8">` 和 viewport 标签，手机上要能看（窄屏一列排开，不出现横向滚动条）。
- 四个页面固定：index.html（活动日程）、books.html（书单）、signup.html（报名说明）、venue.html（场地地图）；加页面要同时改所有页面的导航。
- 改完任何页面都要跑一遍 `python tools/check_site.py`，零错误才能提交。
- 不放任何真实个人信息：联系方式只写“到活动现场找志愿者”或群内说明。
- 回复一律用简体中文；改文件之前先说一句打算改哪里。
